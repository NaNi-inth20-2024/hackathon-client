type UserStatistics = {
    average_grade: number;
    completed_tasks: number;
    active_tasks: number;
    completed_tasks_ratio: number;
    completed_subjects: number;
    unfinished_subjects: number;
    completed_subject_ratio: number;
};

export type { UserStatistics };
